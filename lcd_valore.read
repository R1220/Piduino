#include <LiquidCrystal.h>
LiquidCrystal lcd (6, 7, 5, 4, 3, 2);

int analogPin = 3;

int val = 0;

void setup()

{

	Serial.begin(9600);
	lcd.begin (16, 2);
	lcd.print ("Analog Read");

}

void loop()

{
	val = analogRead (analogPin);

	lcd.setCursor (0, 1);
	lcd.print (val);
}

